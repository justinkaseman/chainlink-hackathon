# chainlink-hackathon

### Starting local GETH

```
geth console --ropsten --syncmode light --ws --ipcdisable --wsaddr 0.0.0.0 --wsorigins="\*"
```

### Run chainlink node

- make install
- sudo ./chainlink node start
